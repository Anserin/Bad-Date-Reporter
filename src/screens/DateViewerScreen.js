import React from "react";
import {View, Button, Text, StyleSheet, TouchableOpacity} from "react-native";

const DateViewerScreen = function({navigation}) {

  return <Text>
      View Reports
  </Text>
};

export default DateViewerScreen;
