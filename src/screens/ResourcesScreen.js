import React from "react";
import {View, Button, Text, StyleSheet, TouchableOpacity} from "react-native";

const ResourcesScreen = function({navigation}) {

  return <Text>
      Hello
  </Text>
};

export default ResourcesScreen;
